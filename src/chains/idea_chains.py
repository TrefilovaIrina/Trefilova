import json
import logging
from typing import Dict, Any, List, Optional
import os

from langchain_core.prompts import PromptTemplate
from langchain_core.runnables import RunnablePassthrough
from langchain_core.output_parsers import JsonOutputParser
from langchain_openai import ChatOpenAI

from src.models.idea_models import Idea, IdeaGenerationRequest, IdeaGenerationResult
from src.models.creative_approaches import get_all_approaches, get_approach_by_name
from src.config import OPENAI_MAX_TOKENS, OPENAI_MODEL, OPENAI_TEMPERATURE

logger = logging.getLogger(__name__)

class IdeaChains:
    """Класс для создания цепочек генерации идей."""
    
    def __init__(self, api_key: Optional[str] = None, model_name: str = OPENAI_MODEL, temperature: float = OPENAI_TEMPERATURE):
        """Инициализация цепочек для генерации идей."""
        self.api_key = api_key or os.getenv("OPENAI_API_KEY")
        if not self.api_key:
            raise ValueError("API ключ OpenAI не найден. Укажите его через параметр api_key или переменную окружения OPENAI_API_KEY")
            
        self.llm = ChatOpenAI(
            openai_api_key=self.api_key,
            model_name=model_name,
            temperature=temperature,
            max_tokens=OPENAI_MAX_TOKENS,
            request_timeout=30,
            max_retries=3
        )
        
        self.output_parser = JsonOutputParser()
        logger.info("IdeaChains успешно инициализирован")
    
    def get_approach_text(self, approach_name: str) -> str:
        """Возвращает статичный текст для выбранного подхода."""
        approach_texts = {
            "triz": """
Ты — эксперт по ТРИЗ и инновационному решению задач.
Твоя задача — проанализировать ситуацию и предложить креативные решения, используя принципы ТРИЗ.

Контекст задачи:
{idea}

Принципы ТРИЗ, которые нужно учитывать:
•	Объективность законов развития системы
•	Преодоление противоречий
•	Идеальность (максимум результата при минимуме затрат)
•	Конкретность (учёт особенностей системы)
•	Сегментация
•	Объединение
•	Вынесение
•	Местное качество
•	Копирование
•	Обратить вред в пользу

Твоя структура ответа:
1.	Определи ключевое техническое или управленческое противоречие в задаче.
(Что мешает достичь цели? Какие два требования конфликтуют?)

2.	Сгенерируй минимум 3 идеи решения, опираясь на разные принципы ТРИЗ.
Для каждой идеи:
– Опиши кратко саму идею.
– Укажи, какой принцип ТРИЗ ты применил.
– Объясни, почему эта идея может быть эффективной.
– Покажи, как она использует ресурсы уже существующей системы.

3.	Сделай вывод в формате бизнес-резюме:
Мы полагаем, что **[предложенное решение / идея]**, 
для целевой аудитории **[кто именно будет использовать / получать выгоду]**, 
чтобы добиться **[желаемый эффект или поведение]**, 
результат измерим по **[метрика / способ измерения эффективности]**. 
Положительное влияние: **[перечисли положительные эффекты]**. 
Отрицательное влияние: **[перечисли возможные риски или ограничения]**.

Формат вывода:
Ответ должен быть логичным, хорошо структурированным, с короткими абзацами. Используй маркированные списки и жирные подзаголовки для читабельности.
Пиши как эксперт, но понятно для бизнес-аудитории.
""",
            "six_hats": """
Ты — команда, проводящая обсуждение задачи по методу "Шесть шляп мышления".
Разбери ситуацию, последовательно "надевая" каждую из шляп. Цель — рассмотреть задачу с разных точек зрения и сформулировать осмысленные, сбалансированные рекомендации.

Контекст задачи:
{idea}

Твоя структура ответа:
1. Белая шляпа (Факты и данные):
– Какие известны факты, цифры, данные?
– Чего не хватает? Какие данные нужно получить?

2. Красная шляпа (Эмоции и интуиция):
– Какие чувства вызывает ситуация у участников?
– Какие интуитивные отклики и предположения могут возникнуть?

3. Чёрная шляпа (Критика и риски):
– Что может пойти не так?
– Какие слабые места, ограничения, угрозы?

4. Жёлтая шляпа (Позитив и возможности):
– В чём преимущества и ценность решения?
– Какие перспективы открываются?

5. Зелёная шляпа (Творчество и альтернативы):
– Какие нестандартные идеи можно предложить?
– Как иначе можно подойти к решению? Какие альтернативные подходы?

6. Синяя шляпа (Модерация и итоги):
– Какова цель обсуждения?
– Какие ключевые выводы можно сделать по итогам?
– Какие шаги предлагается предпринять дальше?

Формат вывода:
Каждую шляпу начинай с её названия и короткого описания роли. Используй маркированные списки, структурированное оформление, логическую последовательность. Пиши ясно, аргументированно, как опытная команда фасилитаторов и аналитиков.

Бизнес-резюме:
Мы полагаем, что **[предложенное решение / идея]**, 
для целевой аудитории **[кто именно будет использовать / получать выгоду]**, 
чтобы добиться **[желаемый эффект или поведение]**, 
результат измерим по **[метрика / способ измерения эффективности]**. 
Положительное влияние: **[перечисли положительные эффекты]**. 
Отрицательное влияние: **[перечисли возможные риски или ограничения]**.
""",
            "mind_map": """
Ты — эксперт, умеющий строить ментальные карты (mind maps) по заданной теме.
Твоя задача — создать иерархически структурированную карту, которую легко визуализировать. В центре — ключевая идея. От неё идут основные ветви, от которых — подробности, идеи, методы, примеры, ассоциации.

Ключевая идея:
{idea}

Твоя структура:
1.	Определи 4–6 основных направлений, связанных с темой.
2.	Для каждого направления создай минимум 3 подветви:
– Это могут быть конкретные идеи, методы, инструменты, примеры, подзадачи и др.
3.	Оформи результат в виде иерархического списка:

# {Ключевая идея}
## 1. Направление 1
- Подветвь 1
- Подветвь 2
- Подветвь 3
## 2. Направление 2
...
""",
            "scamper": """
Ты — эксперт по креативному мышлению и продуктовому развитию.
Твоя задача — применить метод SCAMPER для генерации идей и поиска новых решений.

Ключевая идея / проблема:
{idea}

Формат ответа:
# 🎯 Проблема / Идея: {idea}

## 🔁 S – Substitute (Замени)
- Что можно заменить в процессе / элементе / структуре?
- Какие альтернативы можно использовать?

## ➕ C – Combine (Комбинируй)
- Что можно объединить?
- Какое необычное сочетание даст результат?

## 🔄 A – Adapt (Адаптируй)
- Что из других сфер можно применить здесь?
- Как изменить текущую идею, чтобы она сработала лучше?

## 🔧 M – Modify / Magnify (Модифицируй / Увеличь)
- Что можно усилить, улучшить, "прокачать"?
- Как усилить эффект или удобство?

## 🔁 P – Put to Other Use (Используй по-другому)
- Где ещё можно применить эту идею?
- Как превратить проблему в новый продукт?

## ❌ E – Eliminate (Устрани)
- Что можно убрать?
- Без чего можно обойтись, упростив систему?

## 🔀 R – Rearrange (Переставь)
- Что, если поменять порядок действий?
- Что, если изменить структуру или сценарий использования?

## ✅ Вывод:
- Какие из идей кажутся самыми перспективными?
- Какие шаги можно попробовать уже сейчас?

## 📊 Бизнес-резюме:
Мы полагаем, что **[решение / идея]**, 
для целевой аудитории **[описание аудитории]**, 
чтобы добиться **[цель / желаемое поведение / эффект]**, 
результат измерим по **[метрика / способ измерения]**. 
Положительное влияние: **[перечисли эффекты]**. 
Отрицательное влияние: **[возможные риски или минусы]**.

Формат вывода:
Используй эмодзи для визуального разделения разделов. Каждый раздел должен быть четко структурирован с маркированными списками. 
Пиши кратко и по существу, фокусируясь на практических идеях и их бизнес-применимости.
""",
            "reverse_brainstorming": """
Ты — эксперт по креативному мышлению и нестандартному решению задач.
Применяй метод обратного мозгового штурма: сначала ищем худшие решения, а потом — превращаем их в лучшие.

Тема или проблема:
{idea}

Формат ответа:
# 🎯 Проблема
Как…

# 🔄 Перевёрнутая проблема
Как сделать так, чтобы всё было ещё хуже? (намеренно)

# ❌ Идеи как испортить всё
1. ...
2. ...
3. ...
4. ...
5. ...
(можно больше — до 10)

# ✅ Переворачиваем в позитив
1. ...
2. ...
3. ...
4. ...
5. ...

# 💡 Реальные решения
1. ...
2. ...
3. ...
4. ...
5. ...

# 📊 Итоги
Мы полагаем, что **[решение / идея]**, 
для целевой аудитории **[описание аудитории]**, 
чтобы добиться **[цель / желаемое поведение / эффект]**, 
результат измерим по **[метрика / способ измерения]**. 
Положительное влияние: **[перечисли эффекты]**. 
Отрицательное влияние: **[возможные риски или минусы]**.

Формат вывода:
Используй эмодзи для визуального разделения разделов. Каждый раздел должен содержать нумерованный список идей.
В разделе "Идеи как испортить всё" не ограничивай себя — чем больше идей, тем лучше.
При "переворачивании" в позитив старайся сохранить суть идеи, но найти в ней полезное применение.
В итоговом разделе обязательно укажи конкретные метрики для измерения успеха.
""",
            "walt_disney": """
Ты — команда, применяющая метод Уолта Диснея для поиска креативных, реалистичных и жизнеспособных решений.
Рассмотри задачу с трёх точек зрения: Мечтатель → Реалист → Критик, чтобы пройти путь от амбициозной идеи до конкретного плана действий с учётом ограничений.

Проблема или идея:
{idea}

Формат ответа:
# 🌟 Мечтатель (Visionary)
- Представь, что нет ограничений: ни бюджета, ни времени, ни рисков.
- Какие самые амбициозные, вдохновляющие и необычные идеи приходят в голову?
- Как могло бы выглядеть **идеальное решение**, если бы всё было возможно?

# 🛠️ Реалист (Implementer)
- Какие из идей мечтателя можно **реально воплотить**?
- Какие ресурсы, инструменты и шаги для этого потребуются?
- Как можно адаптировать идею к текущим условиям, не теряя её силу?

# 🔍 Критик (Evaluator)
- Где слабые места и потенциальные провалы?
- Какие **риски, ограничения и ошибки** могут возникнуть при реализации?
- Что нужно предусмотреть, чтобы минимизировать возможные проблемы?

# 📋 Консолидация (Итоги и действия)
- Какие идеи прошли проверку и имеют потенциал?
- Какой конкретный **план действий** можно начать реализовывать уже сейчас?

# 📊 Выводы
Мы полагаем, что **[решение / идея]**, 
для целевой аудитории **[описание аудитории]**, 
чтобы добиться **[цель / желаемое поведение / эффект]**, 
результат измерим по **[метрика / способ измерения]**. 
Положительное влияние: **[ключевые выгоды]**. 
Отрицательное влияние: **[возможные риски и ограничения]**.

Формат вывода:
Используй эмодзи для визуального разделения ролей. Каждый раздел должен содержать маркированные списки с ключевыми пунктами.
Выделяй важные моменты жирным шрифтом. В разделе "Консолидация" обязательно укажи конкретные шаги и сроки.
В итоговом разделе фокусируйся на измеримых результатах и балансе между выгодами и рисками.
""",
        }
        
        return approach_texts.get(approach_name, """
        Проанализируй следующую идею и предложи креативные решения:
        
        {idea}
        
        Структура ответа:
        1. Анализ идеи
        2. Предложенные решения
        3. Рекомендации по реализации
        """)
    
    def create_approach_selection_chain(self):
        """Создает цепочку для выбора креативного подхода."""
        # Получаем все доступные подходы
        approaches = get_all_approaches()
        approaches_text = "\n".join([
            f"- {name}: {approach.description}"
            for name, approach in approaches.items()
        ])
        
        # Создаем шаблон для выбора подхода
        template = """Ты - эксперт по креативным методам решения задач. Тебе нужно выбрать наиболее подходящий креативный подход 
для следующей идеи:

{idea}

Доступные креативные подходы:
{approaches}

Проанализируй идею и выбери ОДИН наиболее подходящий креативный подход из списка выше. 
Объясни, почему этот подход будет наиболее эффективен для реализации данной идеи.

Верни ответ в формате JSON со следующими полями:
- selected_approach: название выбранного подхода (должно точно совпадать с одним из названий в списке)
- explanation: краткое объяснение, почему этот подход подходит для реализации идеи

Пример ответа:
{
    "selected_approach": "scamper",
    "explanation": "Метод SCAMPER идеально подходит для этой идеи, так как позволяет рассмотреть её с разных сторон и найти нестандартные решения."
}"""
        
        prompt = PromptTemplate(
            template=template,
            input_variables=["idea", "approaches"]
        )
        
        # Создаем цепочку
        chain = prompt | self.llm | self.output_parser
        
        return chain
    
    def generate_ideas(self, request: IdeaGenerationRequest) -> IdeaGenerationResult:
        """Генерирует идеи на основе запроса."""
        import time
        start_time = time.time()
        
        try:
            # Выбираем подход для идеи пользователя
            approach_selection_chain = self.create_approach_selection_chain()
            approaches = get_all_approaches()
            approaches_text = "\n".join([
                f"- {name}: {approach.description}"
                for name, approach in approaches.items()
            ])
            
            approach_result = approach_selection_chain.invoke({
                "idea": request.task,
                "approaches": approaches_text
            })
            
            selected_approach = approach_result.get("selected_approach")
            approach_explanation = approach_result.get("explanation", "")
            
            if not selected_approach:
                raise ValueError("Не удалось выбрать подход")
            
            # Получаем подход из списка доступных
            approach = get_approach_by_name(selected_approach)
            if not approach:
                raise ValueError(f"Подход {selected_approach} не найден")
            
            # Создаем идею с выбранным подходом
            idea = Idea(
                title="Идея пользователя",
                description=f"""
                Идея: {request.task}
                
                Выбранный подход: {approach.name}
                Описание подхода: {approach.description}
                Обоснование выбора подхода: {approach_explanation}
                
                {approach.prompt_template}
                """.strip(),
                tags=["идея", selected_approach],
                approach=selected_approach
            )
            
            # Создаем результат
            result = IdeaGenerationResult(
                ideas=[idea],
                selected_approach=selected_approach,
                processing_time=time.time() - start_time
            )
            
            return result
            
        except Exception as e:
            logger.error(f"Error generating ideas: {str(e)}")
            raise 